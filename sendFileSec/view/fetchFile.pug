doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Fetch File
    body 
        include ./includes/header.pug
        form(action="/file/fetchFile",method='POST')
            label(for="a") Input the Code
            br
            input(type="number" name='code' value = code)
            br
            if(locals.error != null && locals.error != '')
                label(for="b") Incurrect password
            else
                label(for="b") Input the password
            br
            input(type="password" id='b' name='password')
            input(type="submit" value='submit')